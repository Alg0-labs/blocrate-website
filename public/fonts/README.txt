Place KLG Kaligra font files here so the hero banner uses the correct font:
- KLG-Kaligra.woff2 (preferred)
- KLG-Kaligra.woff (fallback)

Name the files exactly as above. After adding the file(s), the "KLG Kaligra" font will load automatically.
